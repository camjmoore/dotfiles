-- 'forks' a downloaded colorscheme to a separate and renamed colorscheme file
local minicolors = require("mini.colors")
local colorscheme = minicolors.get_colorscheme("vague", { new_name = "chiroptera" })
-- colorscheme:write()
